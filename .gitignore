# Build output. The workspace shares one target directory at the root.
/target

# Plugin bundles, which `cargo xtask bundle` writes into target/ anyway but
# which people sometimes copy up a level while testing.
/bundled
*.clap
*.vst3

# Editor and OS droppings
*~
*.swp
*.rs.bk
.DS_Store
.idea/
.vscode/

# macOS AU packaging output; keep its documentation tracked.
/dist/
*.component
*.appex
*.app
**/__pycache__/
!/docs/MACOS_AUDIO_UNITS.md
